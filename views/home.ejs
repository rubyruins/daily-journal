<%- include("partials/header") -%>
    <h1>Home</h1>
    <p><%= homeStartingContent %></p>
    <% Posts.forEach(function(post) { %>
        <div>
            <h2><%= post.title %></h2>
            <p><%= post.content %></p>
        </div>
    <% }); %>
<%- include("partials/footer") -%>
